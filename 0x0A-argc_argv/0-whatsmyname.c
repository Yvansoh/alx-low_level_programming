#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * __FILE__ - macro to get name of source file to print
 * Return: 0 if successful
 */

int main(void);
{
	printf("%s\n", __FILE__);
	return (0);
}

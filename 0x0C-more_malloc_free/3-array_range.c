#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * array_range - create an array of integers
 * @min: minimum integer
 * @max: maximum integer
 *
 * Return: integer arrays
 */

int *array_range(int min, int max)
{
	char *output;

	if (min > max)
		return (NULL);

	return (aoutput);
}
